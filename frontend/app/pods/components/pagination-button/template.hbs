<button
  type="button"
  class="btn-inline"
  local-class="btn--{{@arrow}}"
  {{on 'click' @onClick}}
>
  <span>Page {{@pageNum}}</span>
  <svg>
    <use href={{concat "/assets/images/icons.svg#icon-triangle-" @arrow}}></use>
  </svg>
</button>